const serverUrl = 'https://front-test-api.herokuapp.com'
const apiSlug = '/api'

const productsBaseUrl = serverUrl + apiSlug + '/product'
const cartBaseUrl = serverUrl + apiSlug + '/cart'

export { productsBaseUrl, cartBaseUrl }